import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  jumbotron: {
    backgroundColor: 'blue',
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'center',
    marginBottom: 50,
  },
  dpView: {backgroundColor: 'yellow', flex: 1 / 3, marginVertical: 10},
});
export default styles;
